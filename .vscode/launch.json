{
  "version": "0.3.0",
  "configurations": [{
    "type": "node",
    "request": "launch",
    "name": "Launch",
    "program": "${workspaceFolder}/bin/www",
    "stopOnEntry": true,
    "cwd": "${workspaceFolder}"
  }]
}